import React from 'react';
import "./Header.css";

function Header () {
    return (
        <header>
            <h1>Clicky Game!</h1>
            <h2>Click on the images to earn points, but don't click the same image twice!</h2>
        </header>
    );
};

export default Header